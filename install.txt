# Alacritty #

# create directory and link to config file
mkdir .config/alcaritty
ln -sv ~/dotfiles/alacritty.yml ~/.config/alacritty/


# fish #

# download and install
sudo pacman -S fish

sudo apt-get install fish

# omf install
curl -L https://get.oh-my.fish | fish

# remove existing fish config and link dotfiles config folder
rm -R .config/fish
ln -sv ~/dotfiles/fish ~/.config/

# fish prompt and key bindings


# micro #

# for micro to work as $EDITOR, change value in ~/.profile and re-login
